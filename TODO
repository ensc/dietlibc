"locale"

real regerror

Try to improve code size and quality with __restrict__.

FPU exception handling (for mawk)

res_mkquery should not assume op is QUERY

logging.

EINTR handling (*puke*).  What about EAGAIN?

bizarre printf bullshit: "%8.7lx",0xfefe should yield " 000fefe"?!

fix %f and %g in printf.  They are more or less completely b0rken.

gethost/netent -> parselib

s390: shouldn't we zero the frame pointer for gdb?

Look at qsort (make test suite exhibits the worst case)

utmp handling should work read-only, too, if it can't open utmp
read-write
